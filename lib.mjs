export const square = (n) => n * n

export const fahrenheit = (celsius) => celsius * 1.8 + 32

export const rectangleArea = (a, b) => a * b

export const isPalindrome = (str) => str.toLowerCase() === str.toLowerCase().split('').reverse().join('')
